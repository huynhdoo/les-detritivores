export type Example = {
  name: string;
};

export type Discord = {
  success: boolean;
  spotify: string;
  listening_to_spotify: false;
};
export type DiscordUser = {
  username: string;
  public_flags: number;
  id: number;
  discriminator: number;
  avatar: string;
};
